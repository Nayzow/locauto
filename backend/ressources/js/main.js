function loadUpdateLocationModal(idLocation) {
    document.getElementById("idLocation").value = idLocation;
}

function loadUpdateCarModal(idVoiture) {
    document.getElementById("idVoiture").value = idVoiture;
}

function loadUpdateClientModal(idClient) {
    document.getElementById("idClient").value = idClient;
}