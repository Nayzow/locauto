// READ ME
La fonction main se trouve dans l'index.
Le script sql et le cahier des charges se trouvent dans le dossier ressources. Il s'agit du script sql fournit de base.
Le css, le javascript ainsi que les images se trouvent également dans le dossiers ressources.
L'ensemble du code source php se trouve dans le dossier src.
Le fichier .htaccess sert a renvoyé vers l'index si le fichier demandé par l'utilisateur n'est pas l'index.

// Connexion base de données
DB_HOST = 'localhost';
DB_NAME = 'locauto';
DB_USER = 'root';
DB_PASSWORD = '';
